{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "overlay", "region-selector"],
  "permissions": [
    "core:default",
    "opener:default",
    "global-shortcut:default"
  ]
}
